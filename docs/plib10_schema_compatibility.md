# PLIB-10 Schema Compatibility

`tools/test_plib10_schema_compatibility.dart` proves canonical
`ModelPackageManifest` round-tripping and explicit rejection of unknown schema
versions. It is a narrow compatibility fixture, not a promise of stable 1.0
schemas before the planned versioning harness is complete.
