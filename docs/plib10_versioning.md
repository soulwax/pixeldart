# PLIB-10 Versioning

Pixeldart is currently pre-1.0 (`0.1.0`). Public API/schema changes remain
subject to the compatibility harness before any stable release claim. Every
version must have a matching changelog heading; `tools/test_plib10_versioning.dart`
checks that invariant.
